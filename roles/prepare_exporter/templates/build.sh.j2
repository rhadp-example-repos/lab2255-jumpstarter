#!/bin/bash

set -e

rm .python-version
rm  uv.lock

# /home/autom8te/.local/share/uv/python/cpython-3.13.2-linux-aarch64-gnu/bin/python3.13
$HOME/.local/share/uv/python/{{ python_package }}/bin/{{ python_version }} -m venv .venv

source .venv/bin/activate

pip install uv

make && make sync